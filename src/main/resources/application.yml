spring:
  datasource:
    url: jdbc:mysql://localhost:3306/library_online?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=GMT%2B8
    username: root
    password: ZKYANG1239
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
    mapper-locations: classpath:dao/*.xml  #注意：一定要对应mapper映射xml文件的所在路径
    type-aliases-package: com.example.demo.bean  # 注意：对应实体类的路径
